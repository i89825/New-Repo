-
        name: install ngnix
        hosts: localhost
        tasks:
                - name: nginix
                  command: apt-get install filezilla -y
                  become: yes
                - name: version check
                  command: nginx -v
