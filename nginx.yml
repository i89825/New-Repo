-
        name: install ngnix
        hosts: localhost
        tasks:
                - name: nginix
                  command: yum install nginix
                  #um: name = ngnix state = present
                  become: yes
